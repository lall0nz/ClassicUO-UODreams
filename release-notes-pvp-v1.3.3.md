# 0nE UO Launcher v1.3.3 by lall0ne

## Novita' v1.3.3

### Razor Enhanced (fix critico)
- Bundle Razor aggiornato da `0nE-UO-Launcher-v1.2.8-brand-test` (non il Razor vecchio di Downloads/v1.3.0 usato per errore in v1.3.2)
- **OTA aggiorna sempre** gli eseguibili/DLL/plugin Razor in `Assistant` ad ogni Update
- Profilo **Default PVP**: copiato **solo se mancante** — mai sovrascritto se gia' presente
- Profilo stock `default` **mai** toccato da OTA
- GENERAL JSON resta in formato DataTable array `[...]`

### Packaging / OTA
- Zip launcher con entrambi `0nE UO Launcher.exe` e `UODreams Launcher.exe`
- Refresh automatico shortcut Desktop 0nE (icona `oneuo.ico`)
- Pacchetto virgin / pulito (niente settings utente, log, bak)

### Client
- Nessuna modifica client (resta v1.3.1)

> **Nota:** v1.3.2 e' stata ritirata perche' conteneva il Razor sbagliato. Aggiornare a v1.3.3.

---

## English

### Razor Enhanced (critical fix)
- Razor bundle taken from `0nE-UO-Launcher-v1.2.8-brand-test` (not the older Downloads/v1.3.0 Razor mistakenly shipped in v1.3.2)
- **OTA always refreshes** Razor binaries (exes/DLLs/plugins) under `Assistant` on every Update
- **Default PVP** profile: copy-if-missing only — never overwritten if already present
- Stock `default` profile never touched by OTA
- GENERAL JSON stays DataTable array format `[...]`

### Packaging / OTA
- Launcher zip ships both `0nE UO Launcher.exe` and `UODreams Launcher.exe`
- Desktop shortcut refresh to 0nE icon
- Clean virgin package

### Client
- No client changes (remains v1.3.1)

> **Note:** v1.3.2 was withdrawn due to the wrong Razor. Please update to v1.3.3.
